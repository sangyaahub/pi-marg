---
description: Show native OMP capabilities available to the active Auto Mode workflow.
argument-hint: [status | advisor | memory | context]
---

Load `auto-mode-router`, read its native OMP runtime reference, and call `auto_runtime_status`.

- With no argument or `status`, report available tools/commands, active memory, and exact fallbacks without changing configuration.
- With `advisor`, explain `/advisor configure`, `/advisor on`, and `/advisor status` using the saved C/D model policy. Do not rewrite global roles or WATCHDOG files automatically.
- With `memory`, show the approved context-specific local/Hindsight route and `/memory diagnose`. Never expose or write a token.
- With `context`, explain that checkpoint/rewind must be enabled and used only around one disposable exploration branch.

Update the runtime fields in the ledger only after observing the actual state.
