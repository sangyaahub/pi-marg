---
description: Close the current Auto Mode context and begin intake for a different task.
argument-hint: <new work request>
---

If an Auto Mode session is active, preserve its files and mark its status `superseded`; do not delete it. Clear its conversational routing state, load `auto-mode-router`, and begin fresh intake for:

$ARGUMENTS
